"""Briefcase, a local, source-grounded notebook assistant.

Give it your documents; it answers questions using *only* those documents,
with inline citations back to the exact passages, and refuses when the
sources don't support an answer. Inspired by NotebookLM, built to run on a
laptop with no mandatory cloud dependencies.
"""

__version__ = "0.1.0"
